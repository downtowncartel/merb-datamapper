-unless session.authenticated?
  =partial 'sessions/new'
-else
  %span.logout=link_to "Logout", url(:logout)
  %span.alt Hello
  =session.user.name 
%hr
%ul
  %li
    %span=link_to "add a book", url(:new_book)